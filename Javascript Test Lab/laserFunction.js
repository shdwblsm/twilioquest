// Create Function For Laser: OFF

function getLaserSetting() {
  const setting = "OFF"; 
  return setting;
}

const currentSetting = getLaserSetting();
console.log('The current laser setting is: ' + currentSetting);